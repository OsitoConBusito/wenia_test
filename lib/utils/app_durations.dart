class AppDurations {
  /// Very short duration: 100 milliseconds
  static const Duration veryShort = Duration(milliseconds: 100);

  /// Short duration: 300 milliseconds
  static const Duration short = Duration(milliseconds: 300);

  /// Medium duration: 500 milliseconds
  static const Duration medium = Duration(milliseconds: 500);

  /// Long duration: 800 milliseconds
  static const Duration long = Duration(milliseconds: 800);

  /// Very long duration: 1 second
  static const Duration veryLong = Duration(seconds: 1);

  /// Extra long duration: 2 seconds
  static const Duration extraLong = Duration(seconds: 2);

  /// Snackbar duration: 3 seconds, commonly used for Snackbar visibility
  static const Duration snackBarDuration = Duration(seconds: 3);

  /// Animation duration: 250 milliseconds, typical for animation transitions
  static const Duration animationDuration = Duration(milliseconds: 250);

  /// Page transition duration: 300 milliseconds, used for page transitions
  static const Duration pageTransitionDuration = Duration(milliseconds: 300);
}
