part of com.wenia.test.features.favorites.presentation;

class FavoritesScreen extends StatelessWidget {
  const FavoritesScreen({super.key});

  static const String route = 'favorites';

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}
